-- Semilla OPCIONAL de carga (3k). No se incluye en seed.sql ni en SQL CI.
-- Aplicar a mano: psql / supabase db query < supabase/seed-perf.sql

-- INSERT INTO public.whatsapp_conversations (...)
-- SELECT ... FROM generate_series(1, 3000);
SELECT 'seed-perf placeholder: generate locally, never in default seed.sql' AS notice;
